name: Keep Render Server Awake

on:
  schedule:
    - cron: "*/10 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Keep alive Render server
        run: |
          curl -I https://trabek-speech.onrender.com || true
